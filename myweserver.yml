---
- hosts: all
  become: yes
  gather_facts: no
  tasks: 
  - name: install web server on ubuntu ansible nodes
    apt:
      name: apache2
      state: present

  - name: start web server 
    service:
     name: apache2
     state: started
